import React from 'react'

const loginComp = (props) => {
  const {type} = props
  return (
    <div>
<div>
<form onSubmit={()=> {}}>
<button>Send</button>
</form>
</div>      
    </div>
  )
}

export default loginComp
